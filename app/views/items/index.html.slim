.row
  h1.page-header Listing Items

.pull-right
  = form_for_filterrific @filterrific do |f|
    = f.text_field(:search_query, class: 'filterrific-periodically-observed', placeholder: '検索')
    = render_filterrific_spinner

== render partial: 'items/list'

br

= link_to 'New Item', new_item_path
