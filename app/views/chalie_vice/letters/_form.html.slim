= form_for @chalie_vice_letter do |f|
  - if @chalie_vice_letter.errors.any?
    #error_explanation
      h2 = "#{pluralize(@chalie_vice_letter.errors.count, "error")} prohibited this chalie_vice_letter from being saved:"
      ul
        - @chalie_vice_letter.errors.full_messages.each do |message|
          li = message

  .actions = f.submit
